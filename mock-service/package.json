{
  "name": "mock-service",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --host 0.0.0.0",
    "dev": "json-server --watch db.json --port 3001 --host 0.0.0.0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["mock", "api", "segurados", "json-server"],
  "author": "",
  "license": "ISC",
  "description": "Mock service para dados de segurados usando JSON Server",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}

